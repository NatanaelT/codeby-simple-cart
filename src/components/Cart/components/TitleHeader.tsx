import styles from '../Cart.module.css'

export const TitleHeader = () => {
    return (
        <header className={styles.titleContainer}>
            <h1>Meu Carrinho</h1>;
        </header>
    )
}